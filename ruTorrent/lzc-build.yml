# contentdir: 指定打包的内容，将会打包到 lpk 中
# contentdir: ./error_pages

# pkgout: lpk 包的输出路径
pkgout: ./

# icon 指定 lpk 包 icon 的路径路径，如果不指定将会警告
# icon 仅仅允许 png 后缀的文件
icon: ./icon.png

devshell:
  routes:
    - /=http://192.168.33.133:9000
